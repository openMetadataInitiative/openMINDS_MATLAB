classdef ISBN < openminds.abstract.Schema
%ISBN - An International Standard Book Number of the International ISBN Agency.
%
%   PROPERTIES:
%
%   identifier : (1,1) string
%                Enter the numeric commercial book identifier 'International Standard Book Number' (ISBN) following the defined pattern (e.g., 978-4-567-89012-3 (13-digit ISBN) or 4-567-89012-3 (10-digit ISBN)).

%   This class was auto-generated by the openMINDS pipeline

    properties
        % Enter the numeric commercial book identifier 'International Standard Book Number' (ISBN) following the defined pattern (e.g., 978-4-567-89012-3 (13-digit ISBN) or 4-567-89012-3 (10-digit ISBN)).
        identifier (1,1) string ...
            {mustMatchPattern(identifier, '^(?=(?:\d-?){9}-[\dX]$)[\d]{1,5}-[\d]{2,7}-[\d]{1,6}-[\dX]$|^(?=(?:\d-?){13}$)97[89]-[\d]{1,5}-[\d]{1,7}-[\d]{1,6}-[\d]$')}
    end

    properties (Access = protected)
        Required = ["identifier"]
    end

    properties (Constant, Hidden)
        X_TYPE = "https://openminds.om-i.org/types/ISBN"
    end

    properties (Constant, Hidden)
        LINKED_PROPERTIES = struct(...
        )
        EMBEDDED_PROPERTIES = struct(...
        )
    end

    methods
        function obj = ISBN(structInstance, propValues)
            arguments
                structInstance (1,:) {mustBeA(structInstance, 'struct')} = struct.empty
                propValues.?openminds.core.digitalidentifier.ISBN
                propValues.id (1,1) string
            end
            propValues = namedargs2cell(propValues);
            obj@openminds.abstract.Schema(structInstance, propValues{:})
        end
    end

    methods (Access = protected)
        function str = getDisplayLabel(obj)
            str = sprintf('%s', obj.identifier);
        end
    end
end
