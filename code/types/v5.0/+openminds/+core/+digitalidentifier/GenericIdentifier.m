classdef GenericIdentifier < openminds.abstract.Schema
%GenericIdentifier - No description available.
%
%   PROPERTIES:
%
%   emitter    : (1,1) <a href="matlab:help openminds.core.actors.Organization" style="font-weight:bold">Organization</a>
%                Add the organization that governs and/or emits the identifier.
%
%   identifier : (1,1) string
%                Enter a persistent, unique identifier emitted by an organization.
%
%   type       : (1,1) string
%                Enter the type of identifier, e.g. 'PubMed ID'.

%   This class was auto-generated by the openMINDS pipeline

    properties
        % Add the organization that governs and/or emits the identifier.
        emitter (1,:) openminds.core.actors.Organization ...
            {mustBeScalarOrEmpty(emitter)}

        % Enter a persistent, unique identifier emitted by an organization.
        identifier (1,1) string

        % Enter the type of identifier, e.g. 'PubMed ID'.
        type (1,1) string
    end

    properties (Access = protected)
        Required = ["emitter", "identifier"]
    end

    properties (Constant, Hidden)
        X_TYPE = "https://openminds.om-i.org/types/GenericIdentifier"
    end

    properties (Constant, Hidden)
        LINKED_PROPERTIES = struct(...
            'emitter', "openminds.core.actors.Organization" ...
        )
        EMBEDDED_PROPERTIES = struct(...
        )
    end

    methods
        function obj = GenericIdentifier(structInstance, propValues)
            arguments
                structInstance (1,:) {mustBeA(structInstance, 'struct')} = struct.empty
                propValues.?openminds.core.digitalidentifier.GenericIdentifier
                propValues.id (1,1) string
            end
            propValues = namedargs2cell(propValues);
            obj@openminds.abstract.Schema(structInstance, propValues{:})
        end
    end

    methods (Access = protected)
        function str = getDisplayLabel(obj)
            str = obj.identifier;
        end
    end
end
